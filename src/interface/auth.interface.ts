export interface User {
    id: number;
    first_name: string;
    last_name: string;
    email: string;
}

export interface PersonalInfo {
    id: number;
    seeking: boolean;
}

export interface EmailSettings {
    id: number;
}